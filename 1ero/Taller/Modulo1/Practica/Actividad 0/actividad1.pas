program actividad1;
var
  i, j, f, min, max, aux: integer;

begin
  randomize;

  // for j:= 1 to 100 do
  //   begin
  //     i:= random(100);
  //     writeln(i);
  //   end;


  // actividad d

  // i:= random(50);
  
  // writeln('Ingrese el numero minimo que quiere imprimir: ');
  // readln(min);
  // writeln('Ingrese el numero maximo que quiere imprimir: ');
  // readln(max);

  // if (max < min) then
  //   begin
  //     aux:= min;
  //     min:= max;
  //     max:= aux;
  //   end;

  // for j:= 1 to i do
  //   begin
  //     aux:= random(max);
  //     while (aux < min) do
  //       aux:= random(max);
  //     writeln(aux);
  //   end;
  
  // actividad e

  // writeln('Ingrese el numero con el que desea terminar el programa: ');
  // readln(f);

  // writeln('Ingrese el numero minimo que quiere imprimir: ');
  // readln(min);
  // writeln('Ingrese el numero maximo que quiere imprimir: ');
  // readln(max);

  // if (max < min) then
  //   begin
  //     aux:= min;
  //     min:= max;
  //     max:= aux;
  //   end;

  // aux:= random(max);

  // while (aux <> f) do
  //   begin
  //     writeln('N: ', aux);
  //     aux:= random(max);
  //   end;
end.
